				  function update_font_size(divId, lVal){
				    
				      $( divId).css('fontSize', $('#font-size').val() + $('#fontSizeUnit').val());
				    


				  }
				  
				  function update_font_color(divId, lVal){
				    
				      $(divId).css('color', lVal);
				    


				  }
				  function update_font_style(divId, lVal){
				    
				      $(divId).css('fontStyle', lVal);
				    


				  function update_font_weight(divId, lVal){
				  }
				      
				      $(divId).css('fontWeight', lVal);
				    


				  }
				  function update_font_variant(divId, lVal){
				   
				      $(divId).css('fontVariant', lVal);
				    

				  }
				  function update_font_family(divId, lVal){
				  
				      $(divId).css('font-family', $('#font-family').val());
				    

				  }